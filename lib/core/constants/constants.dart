import 'package:flutter/material.dart';

class AppConstants {
  AppConstants._();
  // Global
  static const double bottomNavbarHeight = 40;

  // Padding
  static const kDefaultPadding = EdgeInsets.all(16);
  static const kSmallPadding = EdgeInsets.all(8);
  static const kXSmallPadding = EdgeInsets.all(4);
  static const kLargePadding = EdgeInsets.all(32);
  static const kXLargePadding = EdgeInsets.all(48);

  // Spacings
  static const kDefaultSpacing = 16.0;
  static const kSmallSpacing = 8.0;
  static const kLargeSpacing = 32.0;
  static const kXSmallSpacing = 4.0;
  static const kXLargeSpacing = 48.0;

  // Specific spacings
  static const kTinySpacing = 2.0;
  static const kHugeSpacing = 64.0;
}
